/**
 * Tailwind v4: design tokens live in `app/globals.css` (`@theme`).
 * This file remains for tooling compatibility; keep it minimal.
 */
import type { Config } from 'tailwindcss'

const config = {} satisfies Config

export default config
